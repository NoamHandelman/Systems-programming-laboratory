#include <string.h>
#include <ctype.h>

/**
 * @brief Create a file path with the given filename and extension.
 */
char *create_file(const char *, const char *);

/**
 * @brief Remove empty lines from the given file.
 */

int clear_empty_lines(const char *);
